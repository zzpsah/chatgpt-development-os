# P15 release note draft

DevOS Human Language Execution Engine v2 adds an executable context-aware reference interpreter for short and messy user requests. It can inherit an explicit prior objective for phrases such as `kr do` or `wahi continue`, preserve negative constraints such as `deploy mat karna`, and clarify when a material referent is missing. Interpretation remains non-executing and does not grant authorization.
