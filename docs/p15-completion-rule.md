# P15 completion rule

P15 cannot be called complete merely because files exist. Completion requires fresh pull-request CI evidence showing all newly required language tests and the broader DevOS verification workflow pass. If CI fails, diagnose and repair within scope, then re-run fresh verification.
