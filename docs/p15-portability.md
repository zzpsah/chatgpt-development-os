# P15 portability

The deterministic interpreter consumes a plain JSON envelope and produces plain JSON. It does not depend on ChatGPT account memory or a specific AI vendor. Hosts can construct explicit context from repository-first DevOS recovery state and pass it to the same interpreter contract.
