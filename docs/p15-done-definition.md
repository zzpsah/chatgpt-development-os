# P15 definition of done

The current slice is done only when implementation + tests + safety invariants + CI integration are present and fresh GitHub Actions evidence is green. Merge is followed by durable `.ai` state synchronization; until then P15 remains active.
