# P15 CI scope

The primary contract workflow now runs the existing Human Language Engine verifier plus the v2 contract verifier, contextual interpreter regression test, messy-language corpus, and safety-invariant test before continuing through the existing DevOS contract suite.
