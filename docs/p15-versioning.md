# P15 versioning

The executable envelope identifies itself as `DEVOS-HUMAN-LANGUAGE-v2`. Downstream consumers should treat protocol-version changes as explicit compatibility events rather than assuming all future language output has identical semantics.
