# P15 future slices after the reference interpreter

Once the deterministic v2 baseline is freshly verified, possible later slices include typo/fuzzy normalization, richer Hindi/Devanagari handling, multi-turn correction (`nahi, pehle wala`), ordered multi-intent composition, project-name resolution against the DevOS registry, and optional model-assisted candidate interpretation.

These are not part of the current completion claim unless separately implemented and verified.
