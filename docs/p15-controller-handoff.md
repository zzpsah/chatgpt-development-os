# P15 controller handoff contract

A future controller integration should consume only a `ROUTE` language envelope, then independently resolve/revalidate project state, scope, capability, authorization, Security Gate, and runtime readiness. A language `ROUTE` is not an `EXECUTION_CANDIDATE`.
