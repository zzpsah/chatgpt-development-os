# P15 CI requirement

Required fresh evidence comes from the repository's pull-request workflows. The feature branch is not considered verified merely because its files were successfully written through the GitHub API.
