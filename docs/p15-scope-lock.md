# P15 scope lock

Current PR scope is frozen to the deterministic v2 baseline. Fuzzy typo algorithms, Devanagari normalization, model calls, controller wiring, and project-registry resolution are deliberately deferred until this baseline has fresh CI evidence. This keeps the change bounded and reviewable.
