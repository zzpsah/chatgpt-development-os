# P15 context freshness

Context inheritance is only as reliable as the context supplied by the host. Before a recovered project objective is used for consequential work, existing DevOS repository/context freshness and Git revalidation rules still apply. The language interpreter does not waive stale-head or stale-context checks.
