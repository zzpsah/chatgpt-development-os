# P15 interpretation observability

The interpreter exposes normalized text, selected intents, inherited-context flag, constraints, confidence, ambiguity reasons, and route/clarify decision. This makes interpretation inspectable instead of hiding it in opaque conversational behavior.
