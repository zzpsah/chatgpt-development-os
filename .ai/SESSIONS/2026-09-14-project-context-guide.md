# Session — 2026-09-14 — Plain Project Context and Recovery Guide

## Objective

Provide a normal repository-documentation path for a fresh AI chat without presenting DevOS as a special host mode or authority source.

## Implementation

- Added `DEVOS-PROJECT-CONTEXT.md` with a copy/paste task request, recovery order, and unavailable-context fallback.
- Added `core/devos-project-context-recovery-protocol.md` and deterministic wording checks.
- Linked the guide from `AGENTS.md`, the handoff index, the master map, durable state, and contract CI.

## Boundary

The guide can make DevOS easier to understand for a willing host. It cannot force access, change host policy, grant permission, or prove live compatibility across every AI provider.
