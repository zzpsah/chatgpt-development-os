name: Development OS Context Sync Project Caller

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  sync:
    uses: zzpsah/chatgpt-development-os/.github/workflows/context-sync.yml@main
    with:
      context_directory: .ai
    secrets: inherit
